import React,{useEffect,useState} from "react";
export type CompliancePage="accessibilite"|"mentions-legales"|"donnees-personnelles"|"cookies"|"securite";
const link=(p:string,l:string)=><a href={`?legal=${p}`}>{l}</a>;
export function ComplianceFooter(){return <footer className="compliance-footer" aria-label="Informations réglementaires"><a className="accessibility-status" href="?legal=accessibilite">Accessibilité : non conforme</a><span aria-hidden="true">·</span>{link("mentions-legales","Mentions légales")}<span aria-hidden="true">·</span>{link("donnees-personnelles","Données personnelles")}<span aria-hidden="true">·</span>{link("cookies","Cookies et stockages")}<span aria-hidden="true">·</span>{link("securite","Sécurité")}</footer>}
function Shell({title,children}:{title:string;children:React.ReactNode}){return <div className="compliance-shell"><a className="skip-link" href="#legal-main">Aller au contenu</a><header><div><strong>PCIF Académie</strong><small>Conformité & confiance</small></div><a href="/">Retour à l’application</a></header><main id="legal-main" tabIndex={-1}><h1>{title}</h1>{children}</main><ComplianceFooter/></div>}
const todo=<div className="compliance-warning"><strong>Information à compléter avant une mise en production institutionnelle.</strong><p>Le porteur du service doit valider l’identité du responsable, les coordonnées du DPO, les bases juridiques et les durées de conservation.</p></div>;
function Accessibility(){const[report,setReport]=useState<any>(undefined);useEffect(()=>{fetch('/api/accessibility/report/current').then(r=>r.json()).then(setReport).catch(()=>setReport(null))},[]);return <Shell title="Accessibilité"><p className="legal-lead">PCIF Académie s’engage dans une démarche d’amélioration continue de l’accessibilité numérique.</p><h2>État de conformité</h2>{report?<><p><strong>{report.snapshot?.stats?.rate===100?"Accessibilité : totalement conforme":report.snapshot?.stats?.rate>=50?"Accessibilité : partiellement conforme":"Accessibilité : non conforme"}.</strong></p><p>Audit RGAA {report.snapshot.audit.rgaa_version} publié le {new Date(report.published_at).toLocaleDateString('fr-FR')} — version auditée {report.snapshot.audit.application_version}.</p><p><strong>Taux de conformité : {report.snapshot.stats.rate} %</strong></p><a className="primary-link" href={`?legal=accessibilite&report=${report.slug}`}>Consulter le rapport d’audit détaillé</a></>:<><p><strong>Accessibilité : non conforme.</strong></p><p>Aucun audit RGAA complet publié à ce jour.</p></>}<h2>Schéma pluriannuel et plan annuel</h2><p><a href="/docs/accessibilite-schema-pluriannuel.md">Schéma pluriannuel</a> · <a href="/docs/accessibilite-plan-actions-2026.md">Plan d’actions 2026</a></p><h2>Signaler une difficulté</h2><p>Le canal de contact accessibilité doit être renseigné par le porteur institutionnel du service avant publication.</p></Shell>}
function PublicReport({slug}:{slug:string}){const[r,setR]=useState<any>();useEffect(()=>{fetch(`/api/accessibility/reports/${encodeURIComponent(slug)}`).then(x=>x.json()).then(setR)},[slug]);if(!r)return <Shell title="Rapport d’audit d’accessibilité"><p>Chargement…</p></Shell>;const s=r.snapshot.stats,results=r.snapshot.results,criteria=r.snapshot.criteria;const byTheme=criteria.reduce((a:any,c:any)=>{a[c.theme]??={c:0,nc:0,na:0};const st=results.find((x:any)=>x.criterion_code===c.code)?.status;if(st==='C')a[c.theme].c++;if(st==='NC')a[c.theme].nc++;if(st==='NA')a[c.theme].na++;return a},{});return <Shell title="Rapport d’audit RGAA 4.1.2"><p>Version auditée : <strong>{r.snapshot.audit.application_version}</strong> · publié le {new Date(r.published_at).toLocaleDateString('fr-FR')}.</p><section className="rgaa-summary public"><div><strong>{s.rate}%</strong><span>Taux global de conformité</span></div><div className="bad"><strong>{s.nc}</strong><span>Critères non conformes · {s.blocking} bloquant(s)</span></div><div className="good"><strong>{s.c}</strong><span>Critères conformes</span></div></section><h2>Répartition par thématiques du RGAA</h2><table className="public-audit-table"><thead><tr><th>Thématique</th><th>Conformes</th><th>Non conformes</th><th>Non applicables</th></tr></thead><tbody>{Object.entries(byTheme).map(([k,v]:any)=><tr key={k}><th>{k}</th><td>{v.c}</td><td>{v.nc}</td><td>{v.na}</td></tr>)}</tbody></table><h2>Détails des non-conformités</h2>{results.filter((x:any)=>x.status==='NC').map((x:any)=><article className="public-finding" key={x.criterion_code}><h3>{x.criterion_code} — {x.error_title}</h3><p><strong>Impact : {x.impact==='BLOCKING'?'Bloquant':x.impact==='MAJOR'?'Majeur':'Mineur'}</strong>{x.easy_fix?' · Facile à corriger':''}</p><p>{x.error_description}</p><h4>Correction recommandée</h4><p>{x.recommendation}</p></article>)}</Shell>}
export function CompliancePageView({page}:{page:CompliancePage}){const report=new URLSearchParams(location.search).get('report');if(page==='accessibilite')return report?<PublicReport slug={report}/>:<Accessibility/>;if(page==='mentions-legales')return <Shell title="Mentions légales">{todo}<h2>Éditeur</h2><p>À renseigner par le porteur institutionnel.</p><h2>Hébergement</h2><p>Les informations contractuelles de l’hébergeur doivent être renseignées et validées avant publication.</p></Shell>;if(page==='donnees-personnelles')return <Shell title="Données personnelles">{todo}<h2>Traitements à documenter</h2><ul><li>comptes et authentification ;</li><li>rôles et habilitations ;</li><li>ONF ;</li><li>campagnes et audits ;</li><li>journaux techniques et sécurité.</li></ul></Shell>;if(page==='cookies')return <Shell title="Cookies et stockages"><p>Un inventaire des cookies et stockages locaux doit être maintenu. Les traceurs strictement nécessaires sont distingués des traceurs soumis au consentement.</p></Shell>;return <Shell title="Sécurité"><p>PCIF Académie suit une démarche de sécurité et prépare son évaluation via MonServiceSécurisé.</p></Shell>}
